import { MessageDescriptor } from 'react-intl';

export const messages: { [name: string]: MessageDescriptor } = {
  lastLocation: {
    id: 'side-map.last-location',
    defaultMessage: 'Last location',
  },
};
